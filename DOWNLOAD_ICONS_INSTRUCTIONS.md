# Инструкция по скачиванию иконок из Figma

## ✅ Компонент обновлен

Компонент `FloatingActionBarGold.vue` обновлен и готов использовать иконки в стиле "мел" из Figma.

## 📥 Скачивание иконок

### Вариант 1: Через Figma API (автоматически)

Скрипт автоматически читает токен из `~/.cursor/mcp.json`.

Запустите скрипт:
```powershell
.\scripts\download-figma-icons-api.ps1
```

Токен должен быть настроен в `~/.cursor/mcp.json`:
```json
{
  "servers": {
    "figma-api": {
      "command": "npx",
      "args": [
        "-y",
        "figma-developer-mcp",
        "--figma-api-key=YOUR_TOKEN",
        "--stdio"
      ]
    }
  }
}
```

### Вариант 2: Вручную из Figma (рекомендуется, если нет токена)

Откройте каждую иконку в Figma Dev Mode и экспортируйте как PNG:

1. **WhatsApp** (phone-handset.png):
   - https://www.figma.com/design/dbhRRPEagcwqR97v2vLgAd/ALFA-12.15?node-id=1054-4896&m=dev
   - Выберите элемент → Export → PNG (2x или 3x)
   - Сохраните как `whatsapp.png`

2. **Telegram** (telegram.png):
   - https://www.figma.com/design/dbhRRPEagcwqR97v2vLgAd/ALFA-12.15?node-id=1054-4897&m=dev
   - Выберите элемент → Export → PNG (2x или 3x)
   - Сохраните как `telegram.png`

3. **Phone** (phone-handset.png):
   - https://www.figma.com/design/dbhRRPEagcwqR97v2vLgAd/ALFA-12.15?node-id=1054-4895&m=dev
   - Выберите элемент → Export → PNG (2x или 3x)
   - Сохраните как `phone-handset.png`

4. **Chevron Down** (chevron-down.png):
   - https://www.figma.com/design/dbhRRPEagcwqR97v2vLgAd/ALFA-12.15?node-id=1054-4894&m=dev
   - Выберите элемент → Export → PNG (2x или 3x)
   - Сохраните как `chevron-down.png`

5. Поместите все 4 файла в директорию: `public/images/icons/`

## 📁 Структура файлов

После скачивания структура должна быть такой:
```
public/
  images/
    icons/
      whatsapp.png
      telegram.png
      phone-handset.png
      chevron-down.png
```

## ✅ Проверка

После скачивания иконок компонент `FloatingActionBarGold` автоматически отобразит их на странице `/favorites`.

## 🎨 Особенности дизайна

Иконки имеют уникальный стиль "мел" (chalk-style) с:
- Темным круглым фоном
- Оранжевыми штрихами/заливкой
- Белыми контурами иконок с текстурой

Компонент сохраняет пропорции и использует `object-contain` для правильного отображения.
